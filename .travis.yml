language: ruby
rvm:
  - 2.5.1
    #    deploy:

services:
  - postgresql
  
before_script:
  - cp config/database.yml.travis config/database.yml
  - psql -c 'create database travis_ci_test;' -U postgres


script:
        bundle exec rspec spec --color --format doc
        cucumber features/login_valido.feature
        cucumber features/login.feature
        cucumber features/cadastro_de_usuario.feature

# whitelist
branches:
  only:
    - master
#    - heroku-integration

      #provider: heroku
      #api_key:
      # secure: "YOUR ENCRYPTED API KEY"
